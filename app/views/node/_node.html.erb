<div>
  <h2><%= node.name %> by <%= node.author.name %>, <%= node.written_or_updated %> <%= node.node_time %>
    <% if node.author == current_user %>
        <label for="node-edit">[edit!]</label>
    <% end %>
  </h2>
  <div><%= node.render_body %></div>
  <br>
</div>
